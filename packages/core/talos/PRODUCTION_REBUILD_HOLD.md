# LayerSentry HCI Talos production rebuild hold

The previous r4 ISO workflow is intentionally superseded. Do not treat r4 as a production release.

Release publication remains blocked until the production hardening gates are implemented and pass on `layersentry-platform`, including safe storage ownership behavior, persistent custom installer lifecycle, kernel/extension compatibility, UEFI and installed-disk validation, full runtime inventory, and release checksum verification.
