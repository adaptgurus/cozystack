# LayerSentry Hyper-V console command queue

Each JSON file in this directory represents one immutable installer-console action request. The Windows self-hosted runner processes a newly added command and uploads the resulting console screenshots as workflow evidence.
