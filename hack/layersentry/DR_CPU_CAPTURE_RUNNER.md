# Bounded retained CPU-fixture DR capture runner

Status: SOURCE_COMPLETE. This workflow qualifies the existing libvirt file provider against one explicitly retained, networkless CPU-image fixture; it does not prove native CloudStack recovery, cross-site replication, failover or production readiness. The runtime owner dispatches it only after reviewing the exact successful boot evidence. The worker makes no live dispatch.

The existing root-owned capture harness is pinned at CloudStack `0152e63182e0f01abda723206f57325042a2536f`. It creates a full point, two incrementals, exercises lost-ACK resume and idempotency, rejects a conflicting epoch mode, then boots private materializations of two points and verifies distinguishable marker data. Native CloudStack remains the product authority; this same-host mounted transport is only a provider qualification fixture. No new scheduler, API or storage configuration is introduced.

Inputs bind the exact successful Cozystack boot workflow run, its artifact and runner source commit. The artifact summary, result, cleanup and ownership records must agree with the reviewed CloudStack source and corrected image SHA. The remote preflight requires exactly `.20`, Rocky 9.8, Enforcing, active firewalld/CloudStack management, the owned UUID as the only libvirt domain, a fresh checkpoint-free single disk with no NIC, and at least 30 GiB free. The standard strict verified-host-key SSH profile and global live-environment concurrency lock are retained; the remote deployment lock adds local exclusion. A private exclusive per-source attempt marker prohibits automatic replay after any capture attempt.

Only six required DR Python modules plus their relative boot helper are copied, SHA-256 verified against the exact checkout, and invoked once. A 2400-second capture deadline sends TERM, with a finite cleanup grace and no capture retry. Source guest/catalog/journal state remains retained for the runtime owner's reconciliation; this runner does not remove a guest or replication workspace. It collects explicit progress/results and bounded public recovery reports/journals on both success and failure. It never collects QCOW2 images, arbitrary guest data or private keys. A collector limit/failure is explicit and cannot turn an incomplete acceptance into success.

Rollback means retaining and inspecting the exact owned source/workspace, then using the existing boot ownership cleanup and capture workspace cleanup helpers only after confirming provider state and stopping owned guests. A failed/uncertain capture is never replayed just by rerunning Actions. Package, API, chart, platform and downstream repository contracts are unchanged; no package generation or satellite follow-up applies. Four focused Python tests passed (source/ownership drift, corruption, public-evidence filtering, file trust guards), plus PowerShell boot-binding acceptance and seven negative cases. PowerShell AST, both embedded Python blocks, workflow YAML/source split and git whitespace checks passed. Actual capture is NOT_TESTED until the runtime owner executes the reviewed workflow.

The expected initial boot source remains `9dcd8ea01be5b6463488e7e9660f7106f0f4fdda`; capture source `0152e63182e0f01abda723206f57325042a2536f` includes the recovered public-export readiness correction. These identities are checked separately. Invoke local source checks with `python3 -m unittest discover -s hack/layersentry -p test_dr_cpu_capture_runner.py -v` and `pwsh -NoProfile -File hack/layersentry/test_dr_cpu_capture_binding.ps1`.
