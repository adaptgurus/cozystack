# Module DR host inventory

Status: SOURCE_COMPLETE, live result pending. This read-only continuation reuses the bounded DC collector for the exact DR target 10.10.10.20, retaining 10.10.10.14 as its default. No general remote-command facility, storage operation, service restart or configuration read is introduced. Approved DR credentials and independently verified known_hosts are supplied through existing protected GitHub secrets; strict host checking is mandatory.

The new workflow uses the global LayerSentry live concurrency group and immutable checkout. It reports only allowlisted package/service/network/device/path metadata and sanitized status, never raw SSH diagnostics, private keys or configuration contents. The collector independently requires local target address plus root identity. Python parsing, out-of-scope target rejection, PowerShell parsing and whitespace validation pass; actual runner execution remains pending. Rollback removes the additive workflow/wrapper and restores the collector's old default-only invocation; no guest state needs rollback.

This LayerSentry fork-specific diagnostic does not change Cozystack packages, APIs, schemas, node requirements, chart generation or downstream repository contracts. The upstream/main rebase recipe does not apply to the user's explicitly selected ops/layersentry-hyperv-inventory integration history; no historical reset is performed.

Live read-only run `34049342997` passed at runner `5dd6d7569ac3f8e81556e023674e7d708c73c173` on exact verified-SSH target `10.10.10.20`. Rocky 9.8 / kernel `5.14.0-687.42.1.el9_8.x86_64`; management active, cloudstack-agent/QEMU/libvirt absent, no reported libvirt domains/pools, no bridge ports or primary/secondary/libvirt image directories. The 500 GiB `sdb` reports no filesystem or children. This is prerequisite inventory, not replication or storage readiness.

The next exact-target job installs signed Rocky BaseOS/AppStream/CRB libvirt/QEMU dependencies and starts only local libvirt sockets. It refuses incorrect target/hostname, non-Rocky9, non-Enforcing SELinux, inactive firewall, missing KVM device, existing agent/monolithic daemon/domain configuration, unfinished attempt journals and reused run IDs. It records exact installed versions and compares network/security state. It does not create guests, format disks, configure CloudStack, change bridges/firewall/SELinux or enable remote libvirt listeners. A durable unfinished attempt requires inspection rather than replay. Seven negative/interruption/scope tests pass; Python and PowerShell parse checks pass. Live outcome remains `PENDING` until the exact job completes.

The local-socket design follows [libvirt modular daemon documentation](https://libvirt.org/daemons.html) and [Rocky libvirt setup](https://docs.rockylinux.org/guides/virtualization/libvirt-rocky/). Package installation is a disposable lab prerequisite and does not certify a replication provider. Installed packages are retained for subsequent testing; no automatic package rollback or host reboot occurs.

Prerequisite run `34049966021`, job `101531676220`, installed signed packages and started local sockets, then failed its read-only provider probe. Read-only inspection `34050106742` confirms all three requested sockets active and exact libvirt `11.10.0-12.3.el9_8`, QEMU `10.1.0-17.el9_8.5`, SELinux Enforcing and firewalld active. The missing `virtqemud-ro.socket` is the narrow hypothesis: `virsh --readonly` and `openReadOnly` use that distinct socket, which the initial prerequisite did not start. The explicit reconciliation binds the prior unfinished journal, requires the other sockets still active and the read-only unit loaded/inactive with its listener absent, then starts only that socket and verifies real provider reads. It also installs signed `genisoimage` for the networkless NoCloud acceptance fixture. No initial package installation is replayed. Nine source guard/interruption tests pass; live causal revalidation remains pending.
