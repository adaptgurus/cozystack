# Disabled DC Pod range: lab-only alternative for review

Status: explicit lab-only source and Plan-only strict runner prepared; no Pod request exists. The normal controller still rejects absent capacity rows. Actual capacity observation `34062542683` confirms exact range `.2–.254`, role `0`, VLAN `vlan://untagged` and parent Zone Disabled. Capacity usage is UNKNOWN because native capacity listing excludes Disabled rows; it must never be reported as zero. This design covers only the fixed disposable DC lab while root owns its runtime reservation. The normal production/concurrent-writer path remains blocked on unknown allocation evidence.

## Exact native behavior

CloudStack source is inspected in `/home/opc/layersentry/dr-module-completion` at `1e3b793f0f536678f9b35b31fced75a660dffcb6`. `ConfigurationManagerImpl.findPodAllocationState`8240–8247 inherits parent Zone Disabled, `AlertManagerImpl.createOrUpdateIpCapacity`480–499 writes actual counts with Disabled capacity state, and `CapacityDaoImpl`151–157 excludes those rows. A missing row supplies no usage count.

Automatic console-proxy and secondary-storage scanners use `listEnabledNonEdgeZoneIds` (`ConsoleProxyManagerImpl`1472, `SecondaryStorageManagerImpl`1423), whose DAO386–393 explicitly requires Enabled. This prevents their periodic scan from choosing the present Disabled Zone. It does not prove every administrator operation is disabled: `UserVmManagerImpl`4208 exempts root from the initial Disabled-Zone rejection; `DeploymentPlanningManagerImpl`709–724 has `allow.router.on.disabled.resources` and `allow.admin.vm.on.disabled.resources` overrides; `IpAddressManagerImpl.allocatePodIp`1316–1333 allows root to acquire a Pod address regardless of Disabled. The latter API is synchronous, so an empty async-job list alone cannot exclude that writer.

`ConfigurationManagerImpl.updatePodIpRange`2219 calls `checkIpRangeContainsTakenAddresses`2342–2360, which rejects any currently taken address in the old range that would be outside the new subset. Taken addresses inside `.2–.9` can remain. This check precedes the transaction2233 and Pod lock2282; `DataCenterIpAddressDaoImpl.deleteIpAddressByPodDc`110–116 does not add a taken-null predicate. Therefore the current native implementation is not an atomic fence against a concurrent address allocation. Do not describe its transaction as eliminating that race.

## Proposed bounded protocol

1. Require a SHA-bound reviewed public plan and root's current exclusive lab reservation, explicitly covering all root/admin/API/console/agent writers including synchronous acquirePodIpAddress. The workflow's global lock serializes our jobs but is not claimed to lock outside clients. If any external allocator cannot be excluded, remain blocked.
2. Through the strict known-host DC runner and scoped credentials, verify exact Zone/Pod/cluster/host/management identity; Zone must remain Disabled. Re-list all SystemVM, router and user instances with complete pagination/count evidence and require none. Read both disabled-resource override configs and require explicit false values; missing/unknown values block. Re-list pending async jobs with listall=true and require no pending work before submitting; do not treat this as proof against synchronous external callers.
3. Bind exact current range `.2–.254`, role `0`, untagged VLAN, gateway `.1`, `/24`, and reviewed authoritative DHCP/static exclusions. The candidate `.2–.9` is an existing-range subset outside DHCP `.10–.250` and fixed `.14/.20`; no guessed range, SQL, Zone enable or host routing change is introduced.
4. Record capacityUsed UNKNOWN_DISABLED_ZONE and the explicit administrative-exclusivity dependency. Persist exact old/new endpoints and lab-only binding before one native updatePodManagementNetworkIpRange submission. Native taken-address validation is required unchanged and rejection is a terminal preserved outcome, never bypassed or retried with another range.
5. Record and poll the exact returned async job. On timeout, re-list exact range and query only the recorded job; do not resubmit. Require preserved role/VLAN/gateway/mask and `.2–.9` afterward, no new instances or other pending jobs, and Zone still Disabled. Capacity usage remains UNKNOWN until native visibility can establish a value; deriving eight total addresses is not an observation of eight free addresses.

The proposed result would certify only the fixed range update under an exclusive lab reservation, not free-address count, SystemVM readiness, arbitrary concurrent safety or production readiness. Source implementation and tests must be reviewed before any live Pod Apply. The present controller still rejects absent capacity rows.


## Prepared implementation

`execute_lab` in `dc-pod-range.py` separately implements this fixed lab protocol and retains capacityUsed UNKNOWN_DISABLED_ZONE. Its entry requires exact SHA-256-bound public capacity, completed guest-network and trusted Hyper-V/DHCP receipts plus the explicit root reservation assertion. It freshly checks native scope, both false override configurations and no pending jobs, then supports the existing journaled one-submission mechanism. This does not claim to enforce external administrator exclusivity. `run-dc-pod-range-plan-stdin.py`, `invoke-dc-pod-range-plan-ssh.ps1` and `layersentry-dc-pod-range-plan.yml` expose only Plan, over verified-host SSH with credentials in encrypted stdin and no journal/configuration writes. Apply is rejected by the loader; no executable Apply workflow exists. Ten executed tests cover genuine receipt hashes/tampering, root assertion, override/pending-job gates, unknown evidence, default fail-closed behavior, real journaled async handling and actual PowerShell Legacy private transport. The actual Plan outcome below is available for review; an Apply wrapper remains separately authorized.


## Verified async listing semantics

`ListAsyncJobsCmd` describes pending jobs. Exact `QueryManagerImpl.searchForAsyncJobsInternal`3121–3207 has no explicit jobstatus predicate, but calls `searchAndCount`; `GenericDaoBase`1514–1515 defaults includeRemoved=false, `AsyncJobJoinVO`97 maps the removed timestamp, and normal job completion `AsyncJobManagerImpl`383–394 sets removed inside its completion transaction. Therefore ordinary retained historical completed rows are excluded by the default DAO behavior. `listall=true` expands account/ACL scope and does not include removed history. An anomalous nonremoved terminal row can still appear, so the Plan deliberately blocks every listed job rather than silently treating such a row as absent. Only a recorded own range job is permitted during later reconciliation. Synchronous administrator allocation remains outside this listing and depends on the explicit root reservation. No job-list interpretation or predicate was changed after this trace.


## Actual read-only lab Plan

`34063886863` passed at `30193a35cb1e69736d87430c3582bc927203b6e0`; strict SSH exited zero. The original Plan is preserved byte-for-byte as `dc-pod-reviewed-plan.json`, SHA-256 `240e1b1f9c28e703643ef21d0cf4aecb01ac87ccc4b166978e19a8b0b5bdf665`, with source/run binding in its separate summary under `/tmp/layersentry-dc-pod-plan-34063886863`. The reviewed live code verified exact Disabled DC/Pod/host, role `0`, untagged range `.2–.254`, no SystemVM/user/router instances, explicit false values for both disabled-resource overrides and no listed native jobs. Individual override values and empty job arrays are not serialized separately in this receipt; success is conditional on those executed checks, not an invented additional observation. The result retains capacityUsed UNKNOWN_DISABLED_ZONE, endpoint-derived total253, administrativeExclusivityRequired true and the non-atomic native-guard caveat. configurationUpdateRequested, derivedCapacityRefreshRequested and guestJournalWritesPossible are false. No Apply wrapper/request exists and no Pod allocation changed. The queue was released to root after completion.


## Apply and Observe wiring prepared for source review

`run-dc-pod-range-apply-stdin.py`, `invoke-dc-pod-range-apply-ssh.ps1` and `layersentry-dc-pod-range-action.yml` add only Apply/Observe dispatch capability for this fixed lab. The original Plan bytes must hash to `240e1b1f9c28e703643ef21d0cf4aecb01ac87ccc4b166978e19a8b0b5bdf665` before source loading; all three reviewed prerequisite receipts and the explicit exclusive root/admin/synchronous-writer reservation remain required. Apply stores its private durable journal at `/var/lib/layersentry/native-dc-pod-range-r0`, binding Plan/receipts/endpoint/Zone/Pod/exact parameters/reservation, then calls the unchanged reviewed lab controller. SUBMITTING is durable before the only native mutation. Returned job identity is persisted and retained in public failure summaries; an unresolved intent cannot cause a second submission. The default production capacity path is untouched.

Observe opens only existing directories/lock/journal with no creation, chmod or journal writes; it takes a shared nonblocking lock and validates owner/mode/type/link-count/binding. Missing journal state is explicit. It rechecks current native scope/configurations/instances/jobs, allows only an exactly recorded own job, queries only that job and publishes current old/new endpoints plus unknown usage without modifying the persisted operation. Native API errors remain errors, not authorization to repeat Apply. Observe can report new endpoints after an uncertain submission while preserving the old journal state as evidence. Every result retains the non-atomic guard and administrative-exclusivity caveats.

After source approval and queue release, create exactly one new request in `hack/layersentry/dc-pod-range-actions/` with fields `schema=1`, `target=10.10.10.14`, `mode=Apply` (or separately `Observe`), `planSha256=240e1b1f9c28e703643ef21d0cf4aecb01ac87ccc4b166978e19a8b0b5bdf665`, `reservation=ROOT_EXCLUSIVE_DC_POD_RANGE_INCLUDING_ADMIN_AND_SYNCHRONOUS_IP_WRITERS`, `authorization=DC-POD-EXCLUSIVE-LAB-ACTION`. Commit it as the only new request in the final commit and push the exact commit to `refs/heads/codex/dr-dc-trust`; the workflow checks out github.sha and shares the global live lock. No such request or live Apply is included in this source review. TLS Install retains the current queue reservation.
