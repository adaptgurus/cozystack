# DC host identity through the trusted Hyper-V console

Status: fresh live `EMPTY_LOGIN` confirmed; authentication/public-key verification remains unproved. Base runner is `c8ad45c4dca0755ba3091a4ee3f445e5ba9b8361`, isolated branch `codex/dr-dc-trust`. One authorized Enter refreshed the login; no credential input, SSH authentication or guest configuration changes occurred before the next separately scheduled Login phase.

## Evidence and prerequisite

The source-only DC SSH collector requires independently verified `LAYERSENTRY_DC_SSH_KNOWN_HOSTS`; the lead confirmed this secret is absent. No tracked DC key or fingerprint was found. Existing `layersentry-cloudstack-storage-prep-v2.yml` discards SSH host verification and assumes one VM; its helper stops the registered agent, requires the absent UI RPM, exports broad writable directories and has an unsigned HTTP SystemVM download fallback. It is not the next executable prerequisite path and must not be rerun against the current shared lab.

The lead authorized exact-UUID read-only snapshot run [34046965347](https://github.com/adaptgurus/cozystack/actions/runs/34046965347), source `c8ad45c4d`; it passed. Visual inspection shows Rocky Linux 9.8 and an empty `layersentry login:` interrupted by kernel messages. Image SHA-256 is `6d4362bb6d0fe79b88ec077b24dbceadda16b75add5f173fc39642d4aaabe398`. Prior username-only run `33999764119`, artifact `9979168497`, failed `LOGIN_PROMPT_NOT_VERIFIED` with `inputAttempted=false`, `usernameSent=false`, `passwordSent=false`, `mutationPerformed=false`; its image is identical. Neither screenshot establishes SSH trust or a password prompt.

## Bounded implementation and trust decision

The new `.github/workflows/layersentry-dc-console-trust.yml` has three explicitly selected phases and shares `layersentry-live-environment` serialization. Checkout pins `github.sha`; dispatch selects a phase, while the isolated `codex/dr-dc-trust` push path requires exactly one newly added immutable phase request with the exact target, VM and phase-specific authorization. No push automatically selects a mutation. It binds host `TESTSER`, VM name `sen`, immutable UUID `29ba176b-b81a-4f47-8f51-ecec869f247f` and Running state before observation and each keyboard action. Phase `Observe` reports only a prompt classification. Phase `Login` accepts a freshly observed empty `layersentry` login, optionally refreshes only the known kernel-output shape once, types `root`, observes the new password prompt, submits the environment-only runtime credential once, and observes an empty root shell. Existing/prefilled/unknown password prompts receive no credential. Keyboard input is never retried after any uncertain result.

Phase `Verify` requires an authenticated empty root prompt. `ssh-keyscan` obtains an **untrusted candidate**, without SSH authentication. Its exact `.14` ED25519 algorithm/base64 and binary key structure are validated; this candidate cannot authenticate any session. A fresh 128-bit random challenge and a fixed read-only program are sent through the exact-VM console. The guest independently requires root, confirms local address `.14`, walks `/etc/ssh` through `O_NOFOLLOW` directory FDs, validates root ownership/modes, opens only the regular single-link public host-key file through its directory FD, and compares its algorithm+base64 bytes to the candidate. Symlinks, hardlinks, writable keys/directories, missing identity or mismatches fail closed. No private host key or credential file is read.

The typed command encodes the fixed program, so terminal echo does not contain a plaintext success challenge. Python runs in isolated mode so environment optimization cannot disable its checks. Only executed output clears the screen and renders the challenge and address/root/key comparison result. The public key itself is compared inside the guest and is omitted from the console to avoid 80-column wrapping and fragile base64 OCR; it is retained in `verified_known_hosts` only after the exact challenge receipt succeeds. Exact OCR lines, the fresh random challenge and empty root prompt are required before publishing that file or its public screenshot. OCR uncertainty and unexpected/extra text fail closed. Public-key trust is tied to the Hyper-V identity and console channel; it is not inferred from the unauthenticated network scan.

All working captures are in an ACL-restricted temporary folder outside the upload path and are deleted on exit. Login never uploads any image, OCR transcript, keyboard arguments or raw exception. The workflow publishes only a sanitized stage/outcome; a verified public-key phase additionally publishes the known_hosts line and the allowlisted public screenshot. Logs suppress debug/verbose tracing. Credentials remain environment-only, are not interpolated into shell commands, and are cleared before exit. The workflow does not change guest SSH configuration, authorized_keys, networking, storage, packages, services or repository secrets, and never attempts password SSH.

Live username evidence showed that TypeText is incompatible with this Linux console and must never carry credentials. The script now uses the existing reviewed US-layout virtual-key mapping, extracted unchanged into a pure helper shared with `invoke-hyperv-console-command.ps1`. [Microsoft TypeKey documentation](https://learn.microsoft.com/en-us/windows/win32/hyperv_v2/typekey-msvm-keyboard) defines its virtual-key press/release contract. All ASCII is validated before input; text is bounded at 3500 characters and each key call rechecks VM identity. Shift is released after an uncertain owned operation, but no character or Enter is replayed. Login must observe the exact echoed root username before Enter and a fresh password prompt before sending a credential. [OpenSSH keyscan documentation](https://man.openbsd.org/ssh-keyscan) explicitly requires independent verification; the candidate-to-console comparison supplies that boundary here. A manual trusted console comparison remains an alternative if OCR fails; disabling host checks is not a fallback.

## Tests and next gate

Probe `34050130232` conclusively shows mapped TypeKey delivery: exact root username echo was observed at `17:56:35Z`, Enter completed at `17:56:36Z`, and the public frame at `17:56:37Z` visibly contains the empty Password prompt. Both worker and lead reviewed image SHA-256 `274d45c7f0fca8b8db796963c1c4c1675139c52480322f67af185909457c6bf0`. WinRT reads that word as `Passuord :`; the new alias requires that exact spelling, the preceding root username line and that exact public image hash. Wrong image/username/extra text remain UNKNOWN, and existing password-prompt starts remain ineligible for Login. The lead authorized completion of OOB proof and subsequent verified read-only DC inventory, retaining fresh-empty-login, causal username/password gates, no blind replay and no secret output.

Reviewed virtual-key Login `34049807901` failed at the username transition, with no password sent. Later snapshot `34049896243` shows a clean empty login after timeout but cannot retrospectively establish the missing transition. A new explicitly selected `ProbeLogin` shares Login's exact empty-login and root-echo gates but stops after username Enter and Password observation, never entering credentials. It retains immediate public username-only screenshots/OCR and timestamped transition booleans before getty timeout; Login still keeps all screenshots private. OCR may scale the tiny console bitmap 2x through WinRT, bounded by OcrEngine's documented maximum dimensions; the original screenshot and image hash remain unchanged. The lead authorized one credential-free probe after tests to isolate delivery from OCR.

Login `34049311879` sent only the root username through the old TypeText method and Enter, then failed at `USERNAME_INPUT`, with `passwordSent=false`. Exact read-only snapshot `34049397559`, image SHA-256 `7737d75b4b898ac3bb678c75652aa5e15b141479f5cb5a4e252e2f6464d73d7e`, shows Linux `atkbd` rejecting the literal ASCII codes 0x72/0x6f/0x6f/0x74 instead of entering root, followed by a fresh empty login. No password was sent or exposed. This directly motivates the virtual-key correction above; it is not treated as bad credentials or an OCR failure. No new Login is dispatched until the integration lead reviews the correction and the additional visible username gate.

Refresh `34049039853` sent exactly one authorized Enter from the reviewed image, then failed to classify the resulting prompt; no Enter replay occurred. Exact read-only snapshot `34049108100`, SHA-256 `7d708eacd36cbce11bb05f596173b103b68da0cb6c8a0dcc5757609255e45d88`, visually proves a new empty login below the old kernel messages. OCR had put a far-right kernel continuation after lower terminal rows. Sorting unchanged recognized text by actual bounding-box Y/X coordinates resolves that ordering error; the executed regression also rejects nonempty login. Fresh Observe `34049228743` on `2706f67a42cd5dce8b328c4fce37a17a23a209c9` passed with `EMPTY_LOGIN` and exact VM/keyboard identity, with input/authentication/change flags false. The lead's conditional authorization therefore permits one Login from a newly rechecked empty prompt. Public-key verification still requires a confirmed root shell afterward.

After both OCR fixes, Observe runs `34048469693` and `34048570629` passed on the real Windows runner but correctly reported `UNKNOWN`, with no input. The diagnostic gate refused to publish unknown live OCR because its image differed from the initial reviewed hash. Fresh exact-VM snapshot `34048637562` passed; both the worker and integration lead visually reviewed its image SHA-256 `2b397045222983268bf0807dbbe8db59ae7ab9cfa7e5588494939f0665ecdb8a`: an empty login interrupted by three kernel lines, no username, password or authenticated shell. A separately selected `Refresh` phase requires exactly one of the two reviewed empty-login image hashes and the exact VM, sends exactly one Enter, then only observes an empty login. It never sends credentials or retries input. Arbitrary `UNKNOWN` OCR remains ineligible for Login. The lead explicitly authorized this discrete phase after tests; Login must wait for fresh `EMPTY_LOGIN` evidence.

Refresh `34048780697` accepted the exact reviewed image but failed `KEYBOARD_BINDING_FAILED` before any input. Read-only Observe `34048896943` proved one VM named `sen` and one associated `Msvm_Keyboard`, both identified by uppercase `29BA176B-B81A-4F47-8F51-ECEC869F247F`. The old string comparison incorrectly rejected its casing. The correction compares parsed GUID values and additionally validates the keyboard's own SystemName and class; malformed/different/duplicate identities remain rejected. A regression using the observed uppercase identity failed before correction. The same Observe safely disclosed only the exact reviewed public image's OCR, confirming split/misread kernel lines; the general UNKNOWN classifier remains unchanged. The lead renewed the one-Enter Refresh boundary because the failed attempt had sent no input.

The single authorized Login run `34047745757` failed before guest input inside the existing OCR helper. Observe-only runs `34047899450`, `34048023438` and `34048165739` isolated `CONSOLE_OCR`, nested `MethodInvocationException` (`0x80131501`) and `ArgumentException` (`0x80070057`); all report input/password/SSH/configuration changes and host-trust establishment false. No Login replay occurred. Two actual reflection regressions identify independent helper defects: its broad one-generic/one-argument AsTask selector can select the action-with-progress overload, and its `StorageFile.OpenAsync` conversion incorrectly used `IRandomAccessStreamWithContentType`. The selector now requires `IAsyncOperation<T>`, and the stream result type now matches [Microsoft's documented `IRandomAccessStream` return contract](https://learn.microsoft.com/en-us/uwp/api/windows.storage.storagefile.openasync). Each new regression failed before its corresponding correction. These are source fixes; only a fresh successful live Observe can establish Windows OCR operation and permit coordinated next input.

Executed PowerShell fixtures cover Observe, successful phase transitions, known kernel-output refresh, wrong VM/credential target, existing/prefilled/unknown prompts, uncertain username/password input with no Enter/replay, encoding/chunking, missing authentication and stale/wrong/extra public receipts. The fixed guest payload is also executed with fixture IP/ownership observations and real filesystem FD/link/mode operations, including symlink/hardlink/writable-key/wrong-key/address negatives. These tests do not establish real Windows OCR, console credential delivery or SSH trust. PowerShell parsing and whitespace checks pass. The integration lead must review the final source/test result before coordinating live Login/Verify.

After successful public-key evidence, the lead may provision only that verified known_hosts line into the existing named trust secret, then dispatch `.github/workflows/layersentry-dc-r0-host-inventory.yml`. That collector provides real guest bridge, mount/block-device, NFS, package/service and SystemVM-file observations. Only those fresh observations can define safe storage/Zone/B&R fixture changes. No Hyper-V disk should be reformatted from an old size or device-name assumption; no registered agent should be stopped by the historical storage helper.

Rollback: source removal before deployment; no current guest rollback required. A failed live Login leaves the console state uncertain and must be observed before any next input. A successful Login leaves the root console authenticated for the separately scheduled public-key phase. No automated destructive cleanup, logout/input replay or SSH trust enrollment is performed.
