# DC native TLS installation and activation review

SOURCE_COMPLETE, live Install/Activate/rollback NOT_TESTED. This source preparation does not authorize a dispatch. Root schedules DC current UI deployment first and owns all runtime restart scheduling. Existing preparation run34063409993 at eba8e9772909569cf7d10f974bd27eab20219d54 observed the native certificate job completion, verified server purpose and exact SAN/key/CA binding, and staged native JKS/candidate configuration. Its public leaf DER SHA256 is bf3a4879d200003c1453a2c9be402187c73d7a9e17030dd9fa6b2c9b18f9273f. Original CA SHA256 is67885ce20afcea82d0912937adb7a5d72c2f2cfff7fe894745a5574a4720b1bd. The approved Plan34062801410 SHA is2279b43d2a215a1d7c23fd535ca78c7049b93b15fb79cbd62e605f00881fb105. Original server.properties SHA is3c10e35e00dc1f99f0ed982cf03fa05dd1f09634fc3a6f97ab998a90f64eb159, owner root, group989, mode0640. Hostname layersentry.lab.example and BIOS UUIDccbcac90-c8e3-4091-90a0-7e2e8cf2f7e5 are observed identities, not proposed replacements.

Exact CloudStack4.22.1.1 `client/src/main/java/org/apache/cloudstack/ServerDaemon.java` creates independent HTTP/HTTPS connectors and passes the configured native keystore path/password to Jetty's SslContextFactory.Server and key manager. `utils/src/main/java/com/cloud/utils/server/ServerProperties.java` enables HTTPS only with an existing configured keystore. `packaging/systemd/cloudstack-management.service` runs User=cloud; the wrapper now observes the effective systemd User rather than assuming an unchanged packaged unit. Current cloud group must equal reviewed gid989. No Java options, classpath, HTTP connector settings, service unit or encryption authority is changed.

Install revalidates original server.properties bytes and metadata before either active file is published, so any unexpected DC UI/TLS drift blocks the first mutation. It verifies prepared certificate and staged hashes, root:cloud0640, native restorecon/matchpathcon availability and resulting expected contexts. A service-user `test -r` additionally checks DAC/traversal access without reading or publishing secrets. This does not prove access from the live confined SELinux service domain; actual service startup and authenticated HTTPS must establish that separately. SELinux remains enforcing where configured; no custom policy, permissive mode, port-policy broadening or guessed context is added. A label/ownership/tool failure blocks progress and retains durable per-file intent. The receipt says httpConfigurationPreserved=true and httpListenerVerified=false, avoiding an unobserved listener claim after a file-only phase.

Activate first checks exact installed bytes/metadata and expected SELinux labels. Before its first restart it rechecks native Disabled Zone/no-VM inventory and original CA. A durable restart intent precedes one systemctl restart. Subsequent attempts use fixed local BIOS/NIC/IP/hostname identity and exact artifact checks without requiring the old HTTP API to be ready before startup observation. They only observe the existing restart and return PENDING while unavailable. They cannot submit a second restart. HTTPS success requires actual original-CA trust and exact served leaf, original HTTP8080 success, and a signed native listCaCertificate request over verified HTTPS matching the original CA. Browser/network-client HTTPS remains NOT_TESTED. No firewall opening is enabled by this source.

The locally prepared workflow adds Install and Activate only for the exact retained Plan path `hack/layersentry/dc-native-tls-plans/plan-34062801410.json`, its canonical SHA above and empty firewall_sources. It retains exact repository/ref/manual-event guards. Root must review and push it only after the DC UI receipt proves server.properties preservation. Root then schedules Install once, inspects its public result, and schedules Activate separately. An incomplete restart requires a reviewed observation continuation; a failed invocation is not evidence that restart or file replacement did not happen.

Rollback is implemented as an explicit private-loader operation but is intentionally absent from workflow choices and job guards. Root may invoke it only after separately scheduling recovery through the existing protected Windows SSH wrapper: `& ./hack/layersentry/invoke-dc-native-tls-ssh.ps1 -Mode Rollback -PlanPath hack/layersentry/dc-native-tls-plans/plan-34062801410.json -PlanSha256 2279b43d2a215a1d7c23fd535ca78c7049b93b15fb79cbd62e605f00881fb105`. Reuse the existing protected secret mappings; do not paste any password, API secret, key or configuration bytes into a shell/log. This command is a reviewable recovery procedure, not an instruction to execute now.

Rollback requires the retained original bytes/hash, exact journaled candidate ownership and fixed local identity. It refuses unknown config changes, any prior firewall phase or ambiguous unfinished restore. It journals rollback-config before creating a root-private exclusive `.layersentry-rollback` file, restores only original bytes/owner/group/mode by atomic replacement, applies/verifies native SELinux context, and then journals one rollback-restart. Existing restart intent is observed without replay. Completion requires original HTTP8080 plus authenticated original CA and refused connection on local8443; timeouts are not interpreted as absence. Config/keystore/certificate/journal material is retained, and RootCA is never modified or revoked. Original config restoration permanently blocks another Install/Activate through this journal, so an accidental old phase cannot re-enable the rolled-back candidate. Exact owned keystore cleanup is a separate root-reviewed task; retaining it is safer than deleting evidence during uncertain recovery.

Focused offline tests exercise original-config drift before keystore publish, missing/incompatible tooling before issuance, pending restart observation without HTTP preflight or repeated restart, real file-byte/metadata restoration, refusal of unowned config drift and retained keystore. Existing real OpenSSL/JKS tests continue to verify chain, SAN, key, purpose and store format. They do not substitute for Rocky live label checks or actual management startup. The root-owned ledger should record prepared→installed→locally verified HTTPS only when each corresponding evidence gate passes.
