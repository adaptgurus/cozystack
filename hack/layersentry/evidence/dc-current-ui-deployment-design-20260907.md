# DC current UI deployment handoff

SOURCE_COMPLETE only. These new wrappers have not executed on DC. They reuse `deploy-dr-cloudstack-ui.sh` unchanged at SHA256 `60fa2fe3142e6a8a9c01c3c33c2afab6187984294618d09eff86efeba0bb8f0d` from root runner f120a2803. The reused script independently guards CloudStack4.22.1.1 management RPM ownership, excludes backend WEB-INF/META-INF from asset replacement, fingerprints those trees, preserves runtime API/module config while merging branding, maintains its backup/rollback path, and verifies exact served assets over target-local8080. The historical DR naming of its staging/backup paths is preserved to avoid changing its reviewed behavior; the outer target is strictly DC.

Approved input is UI source `dc58f76f67dac13aa886c8d45475944f31b0c039`, build run34059760313, artifact9997163877. The downloaded artifact ZIP digest is `691f196def90dbfbee9428d33d3077d4879a4668d2d69de582e57e086f2d6451`; the distinct inner `ui-dist.tar.gz` digest, verified against the actual build manifest, is `131717933f8c7fbe4a4385dfa313abd700ffd016926164d825b6bde86aa90edf`. The wrapper requires exact archive digest and manifest fields before SSH. A future root-reviewed workflow must also bind the successful build metadata before downloading this artifact; no new workflow is enabled by this commit.

Use existing protected secret mappings ROCKY_HOST=LAYERSENTRY_ROCKY_R0_HOST, ROCKY_USERNAME=LAYERSENTRY_ROCKY_R0_USERNAME, ROCKY_PASSWORD=LAYERSENTRY_ROCKY_R0_PASSWORD and DC_KNOWN_HOSTS=LAYERSENTRY_DC_SSH_KNOWN_HOSTS. Set the normal GITHUB_RUN_ID, GITHUB_RUN_ATTEMPT, GITHUB_SHA and RUNNER_TEMP through the root-owned scheduled workflow. With the approved artifact already downloaded to a private runner directory, the concrete command is `& ./hack/layersentry/invoke-dc-current-ui-ssh.ps1 -ArtifactDirectory $reviewedArtifactDirectory`. No API/browser credentials are needed. The password remains environment-only behind SSH_ASKPASS, pinned-host SSH allows one password prompt per connection, forwarding of the agent is disabled, and no reusable credential crosses plain LAN HTTP.

Before UI replacement, the target wrapper requires root, exact observed BIOS UUIDccbcac90-c8e3-4091-90a0-7e2e8cf2f7e5, eth0 MAC00:15:5d:00:39:0a enslaved to cloudbr0, and cloudbr0 address10.10.10.14/24. BIOS correspondence to sen VMId29ba176b-b81a-4f47-8f51-ecec869f247f was established by trusted ObserveIdentity run34062671636 at591272077e47d52d52aaf54b3f012bcf4a776520. The wrapper requires rsync already present, preventing the shared helper's optional package installation path. It captures original server.properties bytes, owner/group/mode under root-private staging with exclusive creation, then verifies them after the helper returns, including failure. This preserves the independent native TLS setup. The root scheduling contract must serialize UI restart with native TLS phases and DC fixture operations.

Only allowlisted completion markers and safe hashes/target metadata reach `layersentry-dc-ui-evidence-<run>-<attempt>/summary.json`. No raw native output, runtime config values, browser credentials or private files are published. The remote staging path is retained on success or failure, and the wrapper never automatically retries a deployment. Root should review then clean its exact owned staging after evidence retention. Source tests cover wrong BIOS/VMId/NIC/address rejection, actual config-byte/metadata drift and exclusive backup/symlink rejection. Passing artifact deployment will still report PARTIAL with guiVerified=false and productionCertified=false until independent authenticated GUI acceptance establishes module behavior.
