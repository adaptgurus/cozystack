# DC native recovery fixture reconciliation

Status: native DC storage registration is complete and independently verified. Run `34058013796` passed; fresh API `34058157069` confirms primary storage Up, the NFS image store and the exact official SystemVM template `isready=true`. The source Basic Zone remains Disabled, with no SystemVM or user VM instances. Guest-network setup, address exclusions, SystemVM boot, GUI validation and same-database Advanced recovery are still pending. The next source-only controller adds only a dedicated guest NIC/bridge; no guest-network change has been dispatched.

## Current verified API and host identity

[Post-registration API inventory 34058157069](https://github.com/adaptgurus/cozystack/actions/runs/34058157069), source `f757d2fe754d82b2a533156e0093d39595dc84e4`, confirms the current table below. Overall PARTIAL remains caused by four B&R API 401 configuration gates while the framework is disabled/provider `dummy`; these are not bad-key or absent-repository claims.

| Object | Exact observed identity/state |
| --- | --- |
| Source Zone | `929d1f2c-d629-41ed-96b4-6c48a288d056`, `dc`, Basic, Disabled |
| Pod | `020dc658-af4e-4e2e-bed5-0607de8a787f`, `mgmt`, Enabled |
| KVM cluster | `7d2bb586-6b60-4c51-aa5f-3b4903f31e51`, `abc`, Managed/Enabled |
| KVM host | `3abb80a8-bc4a-48de-bc99-dedc5ac91bf9`, Up/Enabled, `layersentry.lab.example` |
| Primary/image stores | Primary `3ec8a7ea-ebbe-3c45-9f9c-f67d840318b7` Up; NFS image store `e824ee8c-2fa1-466b-9948-629958479a78` registered |
| Existing guest network | `9da9a6de-60f3-4fdb-be41-e3f103f491e7`, Shared/Setup, `10.10.20.0/24`, gateway `10.10.20.1` |
| SystemVM template | `13f22f9c-61e2-4d88-93c0-5735212bccd0`, QCOW2/KVM/SYSTEM, `isready=true`, exact DC Zone and verified official checksum |
| SystemVM/user instances | Fresh API lists empty; SystemVM boot and guest recovery not tested |

## Historical discovery and original proposal (superseded where later evidence differs)

[Verified host inventory 34052767019](https://github.com/adaptgurus/cozystack/actions/runs/34052767019) proves the existing management bridge `cloudbr0` owns `.14/24` and `eth0` forwards on it. Agent/libvirt/NFS services are active. Existing NFS exports `10.10.10.14:/export/primary` and `10.10.10.14:/export/secondary` use the existing root XFS filesystem; there is no evidence requiring a new disk format. The existing SystemVM filename exactly matches the API UUID. This links the objects by name but does not establish image version, integrity or readiness. The existing guest range `10.10.20.0/24` must not be confused with the management subnet `10.10.10.0/24`; guest routing, allocation ranges and SystemVM network reachability remain unverified.

The historical DR thread `01a06d95-801f-74a0-bd9e-bc41aa825b17` was sent an explicit read-only coordination message through `codex queue`. No competing recent live jobs existed before the API dispatch. The separate storage proof uses the global live-environment lock and waits behind the root-owned `.20` boot qualification. No shared integration or historical worktree was edited.

## Historical: Minimal proposed native fixture sequence — not executed

1. Reconcile the observed libvirt pool `9c9fbd8f-e4ee-4e02-9767-6d1cc7a2b8c9` with its public source/target identity and fresh API absence before registration. Do not delete or redefine it merely because API lists are empty. Once reconciled, use native `createStoragePool` for the existing primary NFS URL, exact DC Zone/Pod/cluster above, with cluster scope; use `addImageStore` with provider `NFS`, existing secondary URL and DC Zone. Before each create, re-list by identity and record the request/response in the runtime journal. Existing exports do not require the unsafe historical storage preparation helper.
2. Establish the installed SystemVM image version and a trusted release checksum independently of its generated UUID filename. Observe native image-store/SystemVM template association and readiness after registration. If the existing image cannot be authenticated or is incompatible, propose an isolated supported replacement through the native installation process; do not overwrite the observed image under this read-only task. Verify pod management allocation and actual routes for the configured guest range before enabling the source Zone. An Up KVM host alone does not establish SSVM/console-proxy readiness.
3. Build one disposable Advanced recovery Zone in the **same `.14` management database** using native Zone/physical-network/Pod/cluster/network APIs. Preserve the Basic source Zone. Root must schedule the `.20` agent, bridge and target primary-storage prerequisites around its existing CPU-boot work; an independent `.20` manager does not provide the same native backup/VM records. Choose guest ranges/VLAN/provider settings only from verified available lab inventory. Recovery requires an isolated destination network and explicit source-to-destination network mapping, not reuse of the active source IP/MAC.
4. Enable native B&R at the required global/Zone scopes and select supported `nas` provider. `BackupFrameworkEnabled` is non-dynamic and `BackupManagerImpl.getCommands()` omits B&R commands while disabled; a coordinated management restart and refreshed provider/API observation are therefore part of this gate. Confirm actual installed plugin availability rather than treating the source enum as runtime evidence.
5. Register a dedicated, bounded backup export through native `addBackupRepository` with `type=nfs`, `provider=nas`, source Zone and `crosszoneinstancecreation=true`; the address must be a verified backup path accessible to both KVM hosts. Do not use the primary/image directories as backup contents. A repository on the source-only filesystem cannot establish survival of source-host loss. The two nested VMs share a Hyper-V host, so this lab can prove functional cross-Zone recovery but cannot by itself prove independent-site resilience. Import the repository-backed provider offering, assign a disposable source VM, and create its backup through native APIs.
6. Execute one journaled `createVMFromBackup` into the Advanced destination with explicit network mapping and `preserveip=false`. Verify returned async identity and final VM/volume/network associations before any retry. Submit with `startvm=false` and verify the native result remains Stopped; destination isolation must be verified before any later explicit start. The top-level API description describes the default behavior, but `CreateVMFromBackupCmd` inherits the supported `startvm` parameter and getter from `BaseDeployVMCmd` (lines 198 and 598). `UserVmManagerImpl` sets `ReturnAfterVolumePrepare=true` around line 9812, `VirtualMachineManagerImpl` returns with the VM Stopped at lines 1507–1518, and the later start at `UserVmManagerImpl` line 9847 is conditional on `cmd.getStartVm()`. The earlier unconditional-start inference from the short API description was incorrect; no adapter/provider code was changed. Compare guest application data/checksums against the selected backup, observe source continuity, exercise the intended cleanup/failback path, and capture GUI evidence of source, backup and recovered destination. Only these live results can qualify the native recovery path.

Source basis: CloudStack `api/.../backup/BackupManager.java` defines the non-dynamic framework flag; `server/.../backup/BackupManagerImpl.java:getCommands` gates API registration; `api/.../backup/repository/AddBackupRepositoryCmd.java` exposes cross-Zone repository eligibility; `plugins/backup/nas/.../NASBackupProvider.java` identifies `nas` and derives offerings from repositories. `UserVmManagerImpl.allocateVMFromBackup` loads backup and destination through the same database and supplies network IDs; its Basic allocator rejects explicit network IDs. The existing adapter retains its Advanced-destination guard. No backend core rewrite or readiness bypass is proposed.

## Historical: Storage proof pending

Exact source `be197cdec4dac371f7952a7e52c7991621c1b1fc`; [read-only storage run 34055365447](https://github.com/adaptgurus/cozystack/actions/runs/34055365447). Collector reads only the exact previously observed template directory/image and pool. It refuses symlink traversal, hardlinks, oversized or changing files, projects known public properties and pool fields, and publishes SHA-256 as an observation rather than a release-authenticity claim. Six real-file/hash/path/public-field tests and ten DC inventory tests pass. Final live image hash, pool identity and unresolved version gate will be appended after the serialized run completes.

## Historical: Registration controller prepared for review

`hack/layersentry/register-dc-native-storage.py` reuses the existing native `Client` and POSIX durable `Journal`, and accepts only the fixed DC primary/image registration identities above. It is designed to execute on the verified `.14` guest against loopback `http://127.0.0.1:8080/client/api`, with API credentials injected privately by the future strict SSH launcher. The strict SSH launcher and explicit Plan/Register workflow are now prepared; no registration request file exists and no registration has been dispatched. The parent owns review and the eventual execution boundary.

The CLI requires an existing private persistent journal directory, the reviewed public proof file and its exact independently pinned SHA-256. It checks effective root/local `.14` address, proof identity, the unchanged pre-existing libvirt pool, and current native Zone/Pod/cluster/host scope. By default it emits a proposal. `--execute` permits only `createStoragePool` and `addImageStore`; it cannot enable a Zone, alter networks, restart services, delete/redefine a libvirt pool or format storage. Existing exact storage is reused, including equivalent trailing-slash/default-port NFS endpoint spellings; collisions in another scope, duplicate identities or truncated inventories stop.

Each create has a durable `SUBMITTING` intent before any API call. A timeout or uncertain response is followed only by exact identity re-listing. An empty result leaves the intent unresolved and blocks replay across process restarts. A returned resource ID is checked against the independently listed identity. Registration observation does not claim storage, SystemVM or recovery readiness. Eleven tests execute the actual journal/filesystem path, including intent-before-submit, timeout with and without effect, process restart, equivalent endpoint reuse, conflicting scope, changed pool, replaced resource, and failed journal persistence.

Reviewed future invocation shape (credentials supplied outside command arguments):

```text
python3 register-dc-native-storage.py --journal PRIVATE_PERSISTENT_DIRECTORY --proof-file REVIEWED_PUBLIC_PROOF_JSON --proof-sha256 PINNED_PROOF_SHA256
```

The source also extends the next read-only discovery with fixed pod gateway/netmask/start/end allocation, public VLAN ranges, template checksum, IPv4 main-table routes and the management RPM/NAS plugin package path. Nine executed PowerShell API collector tests and seven storage collector tests pass. These additional fields require fresh runtime evidence before any fixture decision.

## Historical: Native driver collision gate

Root review identified an implicit native mutation that the initial wrapper-only preservation statement missed. `CloudStackPrimaryDataStoreLifeCycleImpl` lines 327–337 computes the NFS UUID with Java `UUID.nameUUIDFromBytes(storageHost + hostPath)`. Exact `10.10.10.14/export/primary` produces `3ec8a7ea-ebbe-3c45-9f9c-f67d840318b7`, which differs from the observed existing pool `9c9fbd8f-e4ee-4e02-9767-6d1cc7a2b8c9`. `LibvirtStorageAdaptor` lines 738–870 can undefine/recreate a conflicting target-path pool or encounter a source conflict. The controller now checks that exact UUID and rejects either target-path collision or same-source/different-UUID collision before submission. The public proof also enumerates all pool names; an unreconciled additional pool blocks the fixed controller. A successful native create may add only its exact deterministic UUID after durable intent, while the old pool's definition must remain unchanged.

If the old pool is the same NFS source under a different UUID, the proposed existing-export registration is **blocked**, not safe merely because the Python wrapper contains no delete command. The next proposal must be either an explicitly reviewed unused-pool metadata migration after verifying no references/mount users, or a separate owned primary subdirectory with its own deterministic identity and verified NFS reachability. Neither migration nor subdirectory creation is authorized or executed by this read-only/source slice. The old pool may instead be unrelated; exact fresh public source/target/type evidence decides that case.

## Completed runtime reconciliation

[Storage proof 34055365447](https://github.com/adaptgurus/cozystack/actions/runs/34055365447) and [combined proof 34056232547](https://github.com/adaptgurus/cozystack/actions/runs/34056232547) both passed with strict verified SSH, exit 0 and `mutationPerformed=false`. The first run completed before the attempted pending-request coalescing check, so it was preserved and never cancelled. The combined run adds all-pool enumeration, routes and management package observations. Exact pool evidence is type `dir`, target `/var/lib/libvirt/images`, UUID/name `9c9fbd8f-e4ee-4e02-9767-6d1cc7a2b8c9`, with no NFS source; it is the only listed pool. It therefore does not collide with the proposed primary NFS source or target. Actual source-only validation of the combined proof passes, with original proof-file SHA-256 `f268bcf25a51e28d33fe475607252d2c8219b51fa9b1f70ea35450c775f2d3a1`.

The DC SystemVM file is root-owned mode 0644, QCOW2 format version 2, no backing file or encryption, physical size 522,649,088 bytes, virtual size 5,242,880,000 bytes, SHA-256 `f2bf62b5c83fc538c01bc3b0693df6dd85dd0d0d3584c459999d70d8a436bb2a`. Its allowlisted metadata is `id=3`, `uniquename=routing-3` and matching image name/sizes; the metadata itself does not state a SystemVM build.

The [official 4.22.1.1 upgrade guide](https://docs.cloudstack.apache.org/en/4.22.1.1/upgrading/upgrade/upgrade-4.22.html) specifies the 4.22.0 SystemVM asset. That [x86_64 KVM asset](https://download.cloudstack.org/systemvm/4.22/systemvmtemplate-4.22.0-x86_64-kvm.qcow2.bz2) was independently streamed over verified HTTPS on the local agent, without guest writes. Its compressed size is 518,074,341 bytes and SHA-512 `10987c4fb3158d40006b1a1db89bf6e9b6c3995294f295e1ca564c26c9543453d4d21a7d8c967d5487b489d88194ecfba9829f00e929b17e3ddaa70751cece90`, matching the [official SHA-512 manifest](https://download.cloudstack.org/systemvm/4.22/sha512sum.txt) and the current API template checksum. Streaming decompression produces the exact observed DC QCOW2 size and SHA-256 above. This establishes byte identity to the documented release asset through HTTPS/checksum comparison; no detached signature was verified, and image-store readiness remains false. The machine-readable comparison is `dc-systemvm-release-comparison-20260906.json`.

[Expanded API inventory 34056240058](https://github.com/adaptgurus/cozystack/actions/runs/34056240058) remains PARTIAL for the disabled B&R APIs, with core inventory available. Pod allocation is `10.10.10.2–10.10.10.254`, gateway `.1`, netmask `/24`; this includes known fixed `.14` and `.20` addresses and must be reconciled before Zone enablement. Guest untagged VLAN range `138d0e03-4dab-4e35-bc5b-4891518f481c` is `10.10.20.2–10.10.20.254`, gateway `10.10.20.1`, network `9da9a6de-60f3-4fdb-be41-e3f103f491e7`, physical network `a3182ad1-7de2-45e3-81ce-5ccbf9280421`. DC main-table routes show only default via `10.10.10.1`, connected `10.10.10.0/24` and `169.254.0.0/16`; guest-gateway reachability remains unproved. Management RPM is `cloudstack-management 4.22.1.1 1`. The allowlisted RPM-path search observed no NAS plugin path; this does not establish that the plugin is absent or loaded.

Artifacts: initial storage `9996000279`, digest `sha256:ae76978d5c9d4e885a92bb0eadf0e2e372aa95a27730a24cdd0b8c6e8d982b18`; combined storage `9996038164`, digest `sha256:c8d541b0ee9df6d7b0efbf91dc7cdb62e996a451972632bd95294358c241b4ff`; expanded API `9996042727`, digest `sha256:b3d67915460fe5fc34ea2c965fac6a25f229041ff85c43607652332c24146277`.

## Historical: Strict registration launcher review boundary

`.github/workflows/layersentry-dc-native-storage-registration.yml` pins checkout to `github.sha`, serializes through `layersentry-live-environment`, defaults to Plan and accepts Register only from an explicit dispatch or a new immutable exact-target authorization request. No such request is included. It references only existing scoped DC SSH, independently verified known_hosts and CloudStack API secrets.

`invoke-dc-native-storage-registration.ps1` uses the proven strict SSH/ASKPASS boundary. The versioned Python sources, pinned original public proof bytes and API credentials are transmitted only through encrypted SSH stdin. Credentials never enter remote command arguments, disk files, journals or artifacts. The guest loader checks target/source digests/proof hash, verifies local DC root/address, and creates or reuses `/var/lib/layersentry/native-dc-storage-r0` as a private persistent journal. Public proof is installed once and cannot be changed on reuse. Symlink and writable-parent traversal are refused. Source/proof/state outputs remain public; raw SSH errors and credential-bearing payloads are withheld. Five launcher tests cover actual PowerShell bundle construction, credential separation, source/target/proof rejection, private directory/path guards and immutable proof reuse. The wrapper has not been run live.

Read-only Hyper-V network discovery is prepared with exact `TESTSER` binding and explicit dispatch support; NAT API failures stop rather than becoming false empty success. It can observe management/guest route and address reservations, but no IP range is assigned by this change. Zone enablement, management restart, network changes, backup registration and any guest restore/start remain outside this registration slice.

## Historical: First registration attempt and held retry

After full lead review of source `70adbc0c765b6ef35e6b617cf4dcc64468bc83f6`, exact TESTSER network inventory `34056883119` passed and no active lab jobs were reported. The lead explicitly authorized one immutable Register request. Run `34056971769`, source `67441e8d0e206f2e3c010d038059b6ab7a6b0472`, failed in the SSH phase with exit 1 and no structured remote result. The absence of a response is not being treated as proof of no mutation. Fresh API run `34057101769` and bounded public journal/storage observation `34057107214` were queued before considering another attempt. No automatic registration replay occurred.

The likely cause is nested quote loss at the Windows native argument boundary in the original `python3 -c` command. A source correction uses quote-free Bash process substitution to supply the reviewed loader through a separate file descriptor, preserving encrypted JSON stdin. The five launcher tests now include executing the actual PowerShell-generated remote command in Bash, proving the loader runs and returns the expected private-input rejection without stderr; this is an executed offline regression, not a live success claim. No new Register request accompanies the correction.

Hyper-V network artifact `9996224970` has digest `sha256:40e73558af07fc98e5849959292a3d599e611ecf5c567bd37fb57e9b6fc61f6a`. Both `sen` and `layersentry-dr-rocky9` have their NIC on `Cozystack-NAT`; host `.1` on that switch is `10.10.10.1/24` and its NAT covers `10.10.10.0/24`. Gateway `10.10.20.1/24` is on the separate `LayerSentry-DR-Internal` switch, with neither VM connected to it. This explains the missing proof of guest gateway adjacency; it does not authorize changing any NIC/switch or assigning an arbitrary address range.

An additional executed PowerShell Legacy native-argument probe reproduced the causal quote loss with a fixed public program: the old command reached an argv-capture executable with the base64 literal's double quotes removed, while the process-substitution command reached it unchanged. The permanent launcher regression now passes the actual generated remote string through that Legacy native boundary before executing it in Bash. This reproduces the launcher defect without credentials or a live guest; runtime journal/API observations remain authoritative for whether the failed request had any effect.


## Reconciled native registration succeeded

The first journal observer was cancelled while pending by shared GitHub concurrency; `cancel-in-progress=false` does not preserve multiple pending jobs. Its attempt 2, run `34057107214`, passed with strict SSH exit 0 and `registrationJournal.status=DIRECTORY_ABSENT`. Fresh API run `34057101769` independently reported no primary/image stores, no VMs or pending jobs, and the Zone still Disabled. The reviewed implementation durably creates its intent before either API submission, so these observations establish the original failed launcher never reached a native submission. The original failure remains preserved.

After explicit lead authorization and checking no active lab jobs, corrected immutable request/source `118a7765556ba9e9a02e4b92b8f5cb5a45f48a51` ran as `34058013796` and passed. Strict SSH returned 0. Primary storage `3ec8a7ea-ebbe-3c45-9f9c-f67d840318b7` reconciled as Up; image store `e824ee8c-2fa1-466b-9948-629958479a78` reconciled with runtime state not reported. The old unrelated dir pool is preserved and the source Zone remains Disabled. No Zone/network/management restart or restore/start was performed. Registration does not establish SystemVM readiness or recovery acceptance.

## Next reviewed network prerequisite path (source proposal only)

Exact Hyper-V evidence binds TESTSER, DC VM `sen` UUID `29ba176b-b81a-4f47-8f51-ecec869f247f`, management NIC ID `Microsoft:29BA176B-B81A-4F47-8F51-ECEC869F247F\174A74C0-18CA-4B09-BC4C-8E9696DEAFE9`, MAC `00155D00390A`, and management switch `Cozystack-NAT` UUID `bbfce11a-d2cf-428f-918b-cb4ccec961a5`. Existing guest switch `LayerSentry-DR-Internal` UUID `3bcf3eab-bc74-44c3-96b6-0064b636525a` has host gateway `10.10.20.1/24`. A minimal next design adds one separately owned guest-only NIC to this exact DC VM and existing guest switch. It does not reconnect the management NIC, create/delete switches, change host NAT, modify DC management routing, or touch the DR VM. The old `invoke-dr-network-isolation.ps1` Apply path is unsuitable because it reconnects the original NIC and requires the VM to be Off.

Before any addition, re-list exact VM/switch/NIC identity, generation, existing MAC-spoof setting, switch VLAN mode and guest device/connection/bridge names. The operation needs a private durable request journal: record before adding, re-list by exact owned adapter identity after uncertainty, never blindly add a second NIC. Capture returned adapter ID/MAC; enable nested guest MAC forwarding only on that owned new NIC. Guest-side strict SSH must bind the new device by this MAC, never guessed `eth1`. Create one owned bridge (proposed `ls-guest0`, only after proving the name unused), with no IPv4/IPv6 address or default route, and enslave only the new device. Preserve management `cloudbr0`, its `eth0` member, `.14/24`, gateway `.1` and strict SSH reachability. Re-list before and after each narrow operation; uncertainty stops further changes. No network changes have been dispatched.

The native source supports a guest label without replacing the agent's management defaults. `NetworkModelImpl.getNetworkTag` lines 1436–1467 obtains the physical Guest traffic type's KVM label; `NetworkOrchestrator` lines 1156–1157 includes it in the NIC profile; `HypervisorGuruBase.toNicTO` line 171 carries its name; `BridgeVifDriver` lines 226–249 uses that explicit label directly as the bridge for untagged Guest traffic. `NetworkServiceImpl.updatePhysicalNetworkTrafficType` lines 5504–5545 updates the traffic record, without a management/agent restart in that method. Thus the proposed final API operation is `updateTrafficType` on the independently observed Guest traffic UUID only, with `kvmnetworklabel=ls-guest0`, after the actual guest bridge is verified. Existing Management/Storage/Public labels must be retained. Whether SystemVM boot needs additional public connectivity remains a separate observation, not a reason to change all labels together.

Pod management allocation `10.10.10.2–10.10.10.254` currently includes fixed `.14` and `.20`. `acquirePodIpAddress` accepts Zone/Pod only and cannot reserve an exact requested address; repeated calls must not be used to guess exclusion. Native `updatePodManagementNetworkIpRange` accepts exact current/new endpoints. `ConfigurationManagerImpl` lines 2191–2240 verifies the existing range and calls `checkIpRangeContainsTakenAddresses` (2342–2360), rejecting a replacement that omits any allocated address. Prefer a reviewed strictly contained range excluding the fixed addresses, after CloudStack taken-address and Hyper-V DHCP/static/neighbor evidence is reconciled. No concrete new range has yet been selected or authorized; absence of ICMP response alone would not prove an address free. An allocation conflict must stop and be investigated rather than bypassed through SQL.

Read-only API discovery now includes actual SystemVM state/address/agent fields, physical network identity/state and each physical network's traffic labels, preserving per-network count/truncation evidence. After native storage/system template observation and reviewed address/guest adjacency changes, Zone enablement is a separate explicit step because it can start SystemVMs. Require console-proxy and secondary-storage VM Running with connected agents, correct allocations and template readiness before progressing. The later recovery Zone must be Advanced and registered in the same `.14` management database, with independent owned storage/network scope and pre-start isolation; the separate `.20` manager does not itself meet that requirement. Root's retained `.20` replication guest and capture workspace must remain undisturbed until their reservation is released.


## Independent post-registration proof

Fresh API run `34058157069`, exact collector source `f757d2fe754d82b2a533156e0093d39595dc84e4`, independently confirms primary storage Up, the new NFS zone image store and **SystemVM template now ready**: template `13f22f9c-61e2-4d88-93c0-5735212bccd0`, zone `929d1f2c-d629-41ed-96b4-6c48a288d056`, `isready=true`, size 5,242,880,000 and the exact official SHA-512 previously verified. There are no SystemVM instances yet and the Zone remains Disabled. Overall inventory is PARTIAL/failure because the four disabled B&R APIs still return 401; core prerequisite observations succeeded without truncation. This is template readiness, not SystemVM boot or GUI/recovery acceptance.

Physical network `a3182ad1-7de2-45e3-81ce-5ccbf9280421` is Enabled with broadcast domain range POD. It has exactly Guest traffic UUID `d33d6182-41c1-4217-9116-bf8234fc1b57` and Management traffic UUID `5f6ff203-7004-42e3-a4f6-10b2aa23ed98`; both current KVM labels are null. Source `ConsoleProxyManagerImpl.getDefaultNetworkForBasicZone` lines 661–675 and `SecondaryStorageManagerImpl` lines 609–622 select the Guest network for the default SystemVM NIC in a Basic Zone. Thus the proposed guest bridge matters to both SystemVM default interfaces, not only tenant VMs. Existing `10.10.20.1` switch gateway adjacency alone does not prove routing, return path or Internet access: the observed host NAT covers only `10.10.10.0/24`. Guest gateway forwarding/egress and console access from the operator must be independently checked before claiming usable SystemVM services; do not silently widen NAT or move management NICs.

Registration artifact `9996568113` digest `sha256:0b3554273879ac554103deb04905a5857849541a72c93356253e2b7c69540465`; independent API artifact `9996608131` digest `sha256:0b97f0f893481ae97213a44337a3dc982f95f4bae4495bd3df795c3061933946`. Public results are downloaded at `/tmp/layersentry-dc-register-34058013796` and `/tmp/layersentry-dc-postregister-34058157069`. The live queue reservation was released to the lead after both completed; no further DC/DR/Hyper-V mutation is running from this slice.


## Guest-network controller prepared; Apply not dispatched

`DcGuestNetwork.psm1`, `invoke-dc-guest-network.ps1`, `dc-guest-network.py` and the strict SSH/private loader implement the reviewed fixed DC sequence. Plan reads the exact Hyper-V and guest/native scope without guest configuration or journal writes. It reports the actual management NIC `MacAddressSpoofing` value; the previous broad inventory omitted that field, so the old evidence alone cannot establish it. Apply requires a SHA-256-bound reviewed Plan receipt showing management spoof already On, the guest NIC absent and the exact VM/switch/management identities. If spoof is Off, Apply stops with a specific separate-review gate; it never changes that setting.

The owned guest MAC is fixed locally administered `02:29:ba:17:6b:81`, checked for collision against all Hyper-V adapters. The [native Add-VMNetworkAdapter command](https://learn.microsoft.com/en-us/powershell/module/hyper-v/add-vmnetworkadapter) supports a specified static MAC. The controller adds only `LayerSentry-DC-Guest-R0` to exact `sen`, initially disconnected. The strict MAC-bound guest phase installs two deterministic NetworkManager profiles for `ls-guest0` and its single new port; the bridge has IPv4 and IPv6 disabled, with no gateway/default route. Only after those profiles reconcile does the host enable MAC spoofing on the owned guest NIC and connect it to the existing guest switch. The final guest phase verifies/activates that owned port/bridge and submits only `updateTrafficType` for the exact Guest UUID. Management label, NIC, bridge, address and all main IPv4 routes are asserted unchanged. No Zone enable, allocation range, NAT, management restart, DR VM or disk operation exists in this controller.

Each create/connect/spoof/native action has intent recorded before submission. Unknown outcomes stop and can only reconcile exact observed identity; an unresolved earlier action is never automatically replayed. Owned NetworkManager autoconnect is explicitly armed before the host supplies carrier, so its expected bridge activation can reconcile without a duplicate activation command. Persistent host and guest journals remain separate from public artifacts and contain no credentials. Ten tests pass with no skips, including actual PowerShell lifecycle execution against constrained Hyper-V fixtures, durable filesystem intent/restart cases, exact MAC/address rejection, and the actual Legacy Windows argument boundary followed by Bash execution of the private loader. Runtime Plan and real NetworkManager behavior still require live observation; no network readiness claim is made.


## Guest-network Plan completed; management spoof already On

Read-only run `34059076116`, source `601a13ab1fae5285625d6c3442faf4ad69bd197e`, passed. The exact management NIC already has `MacAddressSpoofing=On`; no management spoof change is needed. Hyper-V VM/switch/NIC identity checks passed, the guest NIC is absent, and strict DC SSH returned 0 with no guest journal or configuration writes. NetworkManager bridge/profile collision checks and the native Disabled Zone/Guest-label checks passed. Guest label remains null and gateway routing remains NOT_ESTABLISHED. The result is PLAN_REVIEW_REQUIRED, not executed network setup.

The original public Plan receipt is preserved byte-for-byte in `dc-guest-network-reviewed-plan.json`, SHA-256 `f398537bb42e3adb50ee9ba7a0c03f0852d6ea90da04668c6e8670d526e9f053`; a narrow `.gitattributes` entry prevents checkout newline conversion from changing its digest. Artifact `9996883512` has digest `sha256:3d3aeb98580c902cd9b58065641f57577075b01b5f5e8787d8e606e0459a7098`. Presence of this receipt is evidence, not approval to Apply. No Apply request or dispatch exists. The lead now owns the next source/receipt review and runtime scheduling. The live queue reservation was released after this Plan completed.

An additional executed full guest-controller test drives NetworkManager profile creation, explicit owned activation, native Guest-label update and repeated reconciliation while asserting unchanged management identity. The suite now has eleven passing tests with no skips. This fixture test does not substitute for actual NetworkManager activation or packet/GUI evidence.


## Pod fixed-address exclusion proposal (separate from guest-network Apply)

The machine-readable `dc-pod-range-exclusion-proposal-20260907.json` proposes a strictly contained candidate `10.10.10.21–10.10.10.254` from the existing registered `10.10.10.2–10.10.10.254`, excluding known fixed `.14` and `.20` without introducing any new address or subnet. This is not yet an approved free range. Exact DHCP/static/allocation evidence is still required, and no range request is dispatched. Gateway `.1`, CIDR, VLAN and `forsystemvms` must remain unchanged. `listPods` now documents nested `ipranges` as authoritative; its old top-level start/end arrays are deprecated and cannot alone establish range pairing or role.

Native `listCapacity` supports type 5 (PRIVATE_IP), exact Pod/Zone and `fetchlatest=true`. `ManagementServerImpl` lines 3622–3631 invokes recalculation; `AlertManagerImpl` lines 470–499 derives private-IP total/allocated counts from the native DAO and writes derived capacity records. This operation is a diagnostic refresh, not literally zero database writes. A zero fresh allocated count plus consistent no-SystemVM/router/user-instance observations is the proposed simple-controller gate; nonzero or unknown blocks rather than guessing ownership. `ResourceManagerImpl.checkIPConflicts` lines 3634–3659 skips private-IP marking when the host's public and private IPs are equal, so merely seeing `.14` as an Up host does not establish it is reserved. Native `updatePodManagementNetworkIpRange` still checks current taken addresses before its transaction; an allocation conflict must stop with original evidence retained, never be bypassed via SQL or repeated acquirePodIpAddress calls.


## Failed guest Apply reconciled before NIC submission

The approved first guest-network Apply `34059522309`, source `adc55620d1ab2061b4d7407bcfb3951afc2ea091`, passed its fresh strict SSH Plan but stopped inside Hyper-V Prepare before any Bridge/Label guest phase. The generic Apply receipt alone did not prove no mutation. An actual local PowerShell filesystem reproduction identified `File.Replace(tmp,path,$null)` coercing the backup path to an empty string and failing on the second journal save. Source `a9c82f19b` replaces that null argument with a unique real backup path; the executed filesystem regression now covers initial creation and repeated durable replacement, plus safe journal projection.

Combined read-only observer `34059957658`, source `84a021dc7563068f2bec356676a4388de823d542`, passed. Actual durable host journal has `prepareIntent=false`, `spoofIntent=false`, `connectIntent=false`, empty guest ID, exact VM/MAC/Plan bindings. Exact `sen` still has only its original NIC; there is no guest MAC or adapter. This establishes that the failed intent persistence preceded NIC submission. No bridge/profile or native Guest-label phase began. The original failed request is preserved; no automatic Apply replay occurred. Queue was released to the lead's `.20` capture after the observer completed.

## Trusted DHCP and TLS observations change the next prerequisites

The same trusted TESTSER inventory observes active Windows DHCP scope `10.10.10.10–10.10.10.250`, no exclusions and reservations `.11/.12/.13`. Current leases include `.10` for sen MAC `00155D00390A`, `.15` for DR MAC `00155D00390B`, and `.14` for old MAC `00155D003909`; the attached VM inventory contains only current sen/DR MACs. Trusted strict guest evidence establishes static DC `.14` and DR `.20`; DHCP records alone are not their current guest addresses. The previously proposed Pod subset `.21–.254` overlaps this DHCP scope and is rejected before any update. The largest remaining contiguous subset of the existing Pod allocation, disjoint from the trusted DHCP scope and fixed host addresses, is `.2–.9` (eight addresses), with `.251–.254` as a smaller separate remainder. The updated machine-readable proposal selects `.2–.9` subject to fresh native allocation counts and exact range metadata. This derives a range from positive authoritative inventory; it does not rely on ping silence. Static `.14/.20` also need a separately reviewed DHCP exclusion because no current exclusion protects them; no DHCP action or lease revocation has been performed.

Strict DC TLS presence observation reports only a selected `8080` listener, with no `443/8443` listener. nginx, httpd and haproxy have `LoadState=not-found` and expected config paths absent; management is active and its server.properties path exists. Only public paths/status were read, never config/key contents. A trusted HTTPS CloudStack endpoint is NOT_ESTABLISHED; Kubernetes bootstrap TLS must remain required. Any later TLS termination/proxy/certificate setup is a separate prerequisite, not an excuse to weaken bootstrap validation.


## Bounded DHCP and native Pod controllers prepared

`DcDhcpExclusions.psm1` and `invoke-dc-dhcp-exclusions.ps1` prepare exactly two one-address `Add-DhcpServerv4ExclusionRange` operations for `.14` and `.20` in the observed TESTSER scope. They verify the original scope endpoints, exact attached NAT VM identities, host `.1` address and unchanged lease/reservation records. A SHA-bound reviewed Plan is required for Apply. Each exclusion's intent is persisted before its one native submission; uncertainty can reconcile the same observed exclusion but cannot resubmit an absent unresolved one. There is no lease/reservation deletion, scope resize, Pod update or VM/network mutation in this controller. Existing `.14` old-MAC lease remains evidence and is preserved. The [Microsoft command documentation](https://learn.microsoft.com/en-us/powershell/module/dhcpserver/add-dhcpserverv4exclusionrange) confirms one-address exclusions and that reservations override exclusions; therefore a new reservation for either target causes a specific gate, rather than a false claim that an exclusion protects it. Executed PowerShell tests cover both exact additions, preserved leases, repeated reconciliation and ambiguous calls with/without effect. No DHCP Plan or Apply has been dispatched yet.

`dc-pod-range.py` separately implements the native `.2–.9` range operation with exact source Zone/Pod/cluster/host checks, Disabled Zone, no SystemVM/user/router instances, and fresh type-5 capacity zero/expected totals. Native nested `forsystemvms` is serialized as `0`/`1`, not a JSON boolean, and nested VLAN is `vlan://untagged`; `ApiResponseHelper` leaves nested gateway/CIDR unpopulated, so the controller verifies top-level Pod gateway/netmask. Current source is fixed to role `0` pending actual next capacity/range observation. Its async native update persists intent before submission, records returned job identity, polls that job and re-lists exact range/count after uncertainty; no SQL or automatic resubmission exists. Four source tests exercise real journal files, async completion/reconciliation, allocated/unknown count rejection and restart no-replay. The standalone controller is source preparation only; production invocation still requires the reviewed host DHCP evidence and strict runner binding, and no live Pod range request exists.
