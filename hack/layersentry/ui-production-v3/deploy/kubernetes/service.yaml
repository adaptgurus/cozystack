apiVersion: v1
kind: Service
metadata:
  name: layersentry-production-ui
  namespace: layersentry-system
  labels:
    app.kubernetes.io/name: layersentry-production-ui
    app.kubernetes.io/part-of: layersentry
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: layersentry-production-ui
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  sessionAffinity: None
